import { cssTransition } from "react-toastify";

useEffect(()=>{
        fetch_machine();
    },[]);
    const fetch_machine = async () => {
            try{
                const response = await axios.get(URL);
                //console.log('Response : ', response.data);
                setMachine(response.data);
                const data = response.data
                const mids = data.map(m=> m.mid);
                const Tresponse = await axios.get(URL1+mids);
                //console.log('Response : ', response.data);
                setTurbine(Tresponse.data);
            }
            catch(error){
                console.log('Error : ', error);
            }
        }
        
    cssTransition

        @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap');

    body {
        font-family: 'Roboto', sans-serif;
    }

*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, Helvetica, sans-serif;
}

html, body{
    height: 100%;
    width: 100%;
    font-size: 3vw;
}
.b {
    pointer-events: none;
    z-index: 0;
}


.contain{
    
    box-sizing: content-box;
    width: 100vw;
    color: black;
    height: 100vh;
    background-image: url('../blwind.jpg');
    background-size: cover;
    background-position: center;
    overflow: hidden;
    background-size: 105% 120%;
    animation: animateBg 50s infinite alternate ease-in-out;
    z-index: 1;
    
}

.title{
    display: flex;
    font-size: 4vw;
    position: absolute;
    top: 150px;
    left: 100px;
    justify-content: center;
    text-align: center;
    text-shadow: 
    0 0 5px rgba(255, 255, 255, 1), 
    0 0 10px rgba(0, 0, 255, 0.55), 
    0 0 20px rgba(0, 0, 255, 0.324),
    0 0 30px rgba(0, 0, 255, 0.185); 
    font-family: Georgia, 'Times New Roman', Times, serif;

}
.sbtn{
    
    border-radius: 5px;
    font-size: 16px;
    position: absolute;
    top: 230px;
    left: 300px;
    background-color: rgb(11, 62, 231);
    color: white;
    border: none;
    transition: 0.3s ease;
    padding: 10px;
    margin: 10px;
    cursor: pointer;
    pointer-events: auto;
    z-index: 10;
    
}
.sbtn:hover{
    background-color: rgb(83, 120, 241);
}
.contain{
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
}
@keyframes animateBg {
    0%{
        background-position: 0% 0%;
    }
    50%{
        background-position: 100% 0%;
    }
    100%{
        background-position: 0% 0%;
    }
}

glass card 

.glass-card {
  width: 240px;
  height: 360px;
  background: rgba(255, 255, 255, 0.16);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  border-radius: 20px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  box-shadow: 
    0 8px 32px rgba(0, 0, 0, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.5),
    inset 0 -1px 0 rgba(255, 255, 255, 0.1),
    inset 0 0 6px 3px rgba(255, 255, 255, 0.3);
  position: relative;
  overflow: hidden;
}

.glass-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.8),
    transparent
  );
}

.glass-card::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 1px;
  height: 100%;
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.8),
    transparent,
    rgba(255, 255, 255, 0.3)
  );
}